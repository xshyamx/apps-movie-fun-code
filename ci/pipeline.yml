---
resources:
- name: apps-movie-fun-code
  type: git
  source:
    uri: {{github-repository}}
    branch: {{github-branch}}
    private_key: {{github-private-key}}

- name: apps-movie-fun-code-artifacts
  type: s3
  source:
    bucket: {{aws-bucket}}
    regexp: releases/moviefun-(.*).war
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: version
  type: semver
  source:
    bucket: {{aws-bucket}}
    key: apps-movie-fun-code/version
    access_key_id: {{aws-access-key-id}}
    secret_access_key: {{aws-secret-access-key}}

- name: sandbox-deployment
  type: cf
  source:
    api: {{cf-api-url}}
    username: {{cf-username}}
    password: {{cf-password}}
    organization: {{cf-org}}
    space: sandbox

jobs:
- name: build
  plan:
  - get: apps-movie-fun-code
    trigger: true
  - get: version
    params: {bump: patch}
  - task: build and test
    file: apps-movie-fun-code/ci/build.yml
  - put: apps-movie-fun-code-artifacts
    params:
      file: build-output/moviefun-*.war
  - put: version
    params:
      file: version/number

- name: deploy-review
  plan:
  - get: apps-movie-fun-code
    passed: [build]
  - get: apps-movie-fun-code-artifacts
    trigger: true
    passed: [build]
  - put: sandbox-deployment
    params:
      manifest: apps-movie-fun-code/manifest.yml
      path: apps-movie-fun-code-artifacts/moviefun-*.war

